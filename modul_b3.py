# We import only the find_index function
from modul_a import find_index

courses = ["PE", "History", "Math"]
index = find_index(courses, "PE")
print(index)  # Prints 0

# print(modul_a.test)  # nefunguje
